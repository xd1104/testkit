# Render.com 部署設定。連上 GitHub repo 後 Render 會自動讀這個檔。
services:
  - type: web
    name: testkit
    runtime: node
    plan: free
    buildCommand: ""
    startCommand: node server.js
    envVars:
      - key: APP_PASSWORD
        sync: false   # 在 Render 後台手動填密碼，不寫進程式碼
